# MyPhonicsBooks — Python Dependencies

# AI APIs
anthropic>=0.40.0          # Claude — story generation, validation
openai>=1.50.0             # DALL-E 3 — illustration generation

# PDF Generation
reportlab>=4.1             # PDF assembly — primary engine
Pillow>=10.4               # Image processing, resizing, compositing

# Web / API
fastapi>=0.115.0           # Backend API server
uvicorn>=0.32.0            # ASGI server
httpx>=0.27.0              # Async HTTP client

# Database
supabase>=2.9.0            # Production database + storage + auth

# Email
resend>=2.0.0              # Transactional email delivery

# Payments
stripe>=10.0.0             # Payment processing

# Utilities
python-dotenv>=1.0.0       # Environment variable loading
pydantic>=2.9.0            # Data validation and settings
email-validator>=2.1.0     # Required by pydantic EmailStr

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Optional — alternative PDF engine
# weasyprint>=62.0         # HTML/CSS → PDF (if preferred over ReportLab)
# playwright>=1.48.0       # Browser automation for HTML → PDF fallback
